{
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index" }, // Redirect API requests to /api/index
    { "source": "/(.*)", "destination": "/index" } // Redirect other requests to /index
  ]
}
